# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/

# .NET / VS
*.user
.vs/
*.suo
project.lock.json

# Rider / VS Code
.idea/
.vscode/

# OS
Thumbs.db
.DS_Store

# CodeCarver: carved output, scratch corpora, and fetched toolchains are never committed
.carved/
.corpus/
.oracle-cpp/
.toolchains/

# Filled-in presets carry real repo paths/roots — keep only the .example templates
presets/*.ps1
!presets/*.example.ps1

# Release artifacts (produced by release.ps1) are never committed — they live on the git remote's
# releases, traceable by the commit SHA stamped in each zip.
dist/
*.tmp
.claude/scheduled_tasks.lock

# Discordinator MCP: machine-local config (points at a global server + a Discord channel/token);
# never belongs in the repo. The server itself lives outside the tree (a global Python module).
.mcp.json
**/discordinator/

