# Shell scripts must stay LF — CRLF breaks the `#!/usr/bin/env bash` shebang under WSL/Linux
# (`bad interpreter: .../bash^M`). These are run inside WSL by the linker-map / C++ link oracles.
*.sh text eol=lf
